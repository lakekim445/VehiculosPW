@model IEnumerable<VehiculosMVC.Models.Mantenimiento>

@{
    ViewData["Title"] = "Historial de Mantenimientos";
}

<div class="d-flex flex-column flex-sm-row justify-content-between align-items-start align-items-sm-center mb-3 gap-2">
    <h1 class="h3 mb-0">Historial de Mantenimientos</h1>
    <a asp-action="Create" class="btn btn-success">
        <i class="bi bi-plus-circle"></i> Nuevo Mantenimiento
    </a>
</div>

<div class="card shadow-sm">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-striped table-hover align-middle mb-0">
                <thead class="table-dark">
                    <tr>
                        <th>Id</th>
                        <th>Vehículo</th>
                        <th class="d-none d-md-table-cell">Fecha</th>
                        <th>Tipo Servicio</th>
                        <th class="d-none d-lg-table-cell">Descripción</th>
                        <th class="text-end">Costo</th>
                        <th class="text-end">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Id</td>
                            <td class="fw-bold">@item.Vehiculo?.Placa</td>
                            <td class="d-none d-md-table-cell">@item.Fecha.ToShortDateString()</td>
                            <td>@item.TipoServicio</td>
                            <td class="d-none d-lg-table-cell">@item.Descripción</td>
                            <td class="text-end">@item.Costo.ToString("C")</td>
                            <td class="text-end text-nowrap">
                                <a asp-action="Details" asp-route-id="@item.Id"
                                   class="btn btn-info btn-sm" title="Ver">
                                    <i class="bi bi-eye"></i>
                                    <span class="d-none d-lg-inline">Ver</span>
                                </a>
                                <a asp-action="Edit" asp-route-id="@item.Id"
                                   class="btn btn-warning btn-sm" title="Editar">
                                    <i class="bi bi-pencil"></i>
                                    <span class="d-none d-lg-inline">Editar</span>
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.Id"
                                   class="btn btn-danger btn-sm" title="Eliminar">
                                    <i class="bi bi-trash"></i>
                                    <span class="d-none d-lg-inline">Eliminar</span>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>